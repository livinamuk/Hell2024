#pragma once

enum class CSGType {
    ADDITIVE_CUBE,
    SUBTRACTIVE,
    DOOR,
    WINDOW,
    ADDITIVE_PLANE
};

enum class BrushShape {
    CUBE,
    PLANE
};

enum BrushType {
    AIR_BRUSH,
    SOLID_BRUSH
};
